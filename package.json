{
  "name": "sample-schema",
  "version": "1.0.0",
  "description": "Sample database schemas with enum types",
  "scripts": {
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "db-push": "prisma db push",
    "migrate-reset": "prisma migrate reset"
  },
  "devDependencies": {
    "@prisma/client": "^6.14.0",
    "prisma": "^6.14.0"
  }
}
